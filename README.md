# expense-tracker

A command-line interface (CLI) application built with Python to track daily expenses, categorize expenditures, and calculate spending totals.

## Planned Architecture
* Local data persistence using JSON storage.
* Modular CLI arguments via the argparse standard library.
* Features: Add expense, view all transactions, and display calculated totals.